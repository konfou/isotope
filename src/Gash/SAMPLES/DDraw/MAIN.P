class Main extends Object
{

	method Init()
	{
	}

	proc New(Main Dest a)
	{
		Asm.New(Dest)
		Main.Init(Dest)
	}

	proc Main()
	{
		DirectDrawWindow w
		DirectDrawWindow.New(w, 640, 480, 0, 32)
		DirectDrawImage image1
		DirectDrawImage.New(image1, w)
		DirectDrawImage.Load(image1, 'Test.bmp')
		DirectDrawImage canvas
		DirectDrawWindow.getCanvas(w, canvas)
		DirectDrawImage.Blt(image1, 0, 0, 49, 49, canvas, 10, 10)
		PopUps.MessageBox('Hi', 'Ho')
	}

}


